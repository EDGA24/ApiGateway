# ============================================================================
# AUTHENTICATION SERVICE - CONFIGURACIÓN DE ENTORNO
# ============================================================================

# Puerto en el que corre el servicio de autenticación
PORT=3002

# Entorno de ejecución (development, production, test)
NODE_ENV=development

# ============================================================================
# SEGURIDAD - JWT (JSON Web Tokens)
# ============================================================================

# Clave secreta para firmar tokens JWT
# IMPORTANTE: Debe ser la misma que el API Gateway
# IMPORTANTE: Cambiar en producción por una clave segura y aleatoria
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Tiempo de expiración del token (ejemplos: 24h, 7d, 60m)
JWT_EXPIRATION=24h

# ============================================================================
# CORS - Control de acceso de orígenes
# ============================================================================

# Orígenes permitidos (separados por coma)
# En desarrollo: * permite todos los orígenes
# En producción: especificar dominios exactos como https://app.example.com
CORS_ORIGIN=*

# ============================================================================
# COMUNICACIÓN CON MICROSERVICIOS
# ============================================================================

# URL del microservicio de usuarios
# El servicio de autenticación se comunica con el servicio de usuarios
# para validar credenciales y crear usuarios
USERS_SERVICE_URL=http://localhost:3001/api

# ============================================================================
# NOTAS IMPORTANTES
# ============================================================================
#
# 1. JWT_SECRET: DEBE ser el mismo en API Gateway y Authentication Service
# 2. En producción, usar variables de entorno reales, no valores por defecto
# 3. Nunca commitear el archivo .env al repositorio
# 4. USERS_SERVICE_URL debe apuntar al servicio de usuarios correcto
#